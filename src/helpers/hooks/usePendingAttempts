const usePendingAttempts = () => {
  const setPendingUuid = (uuid) => {
    localStorage.set("uuid", uuid);
  };

  const getPendingUuids = () => {
    return localStorage.get("uuids", false);
  };

  return { setPendingUuid, getPendingUuids };
};
